PORT=5000
NODE_ENV=development
MONGODB_URI=mongodb+srv://vadadoriyanency8_db_user:udZUvlpTtd1aM7a4@cluster0.njceucb.mongodb.net/mydb
JWT_SECRET=your_jwt_secret_key_here_change_in_production
JWT_EXPIRES_IN=24h

# Frontend URL
FRONTEND_URL=http://localhost:3000


# vadadoriyanency8_db_user

# udZUvlpTtd1aM7a4

# mongodb+srv://vadadoriyanency8_db_user:udZUvlpTtd1aM7a4@cluster0.njceucb.mongodb.net/?appName=Cluster0

# NOTES:
# 1. Copy this file to a file named `.env` in the backend folder (or project root) and
#    replace the `MONGODB_URI` value with your connection string.
# 2. Common variable names used by the code: `MONGODB_URI`, `MONGO_URI`, or `DATABASE_URL`.
# 3. Keep `.env` out of version control and never commit real credentials.